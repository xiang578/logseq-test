---
public: true
categories: problem
tags:
- LeetCode
- implementation
- Problems/2025
date:
- 2025/01/05
permalink: problem/lc2241
title: LC2241. 设计一个 ATM 机器
lastMod: 2025-01-05
toc: "true"
---

题目链接：[[LC2241. 设计一个 ATM 机器]] [2241. 设计一个 ATM 机器](https://leetcode.cn/problems/design-an-atm-machine/)
<!--more-->
## 题解
按题意模拟，可能写的有点复杂了。。。
## 参考代码
```python
class ATM:

    def __init__(self):
        self.banknotes = {}
        self.a = [20, 50, 100, 200, 500]
        for i in [20, 50, 100, 200, 500]:
            self.banknotes[i] = 0

    def deposit(self, banknotesCount: List[int]) -> None:
        for i in range(5):
            self.banknotes[self.a[i]] += banknotesCount[i]

    def withdraw(self, amount: int) -> List[int]:
        
        ans = []
        tmp = amount
        # print(self.banknotes)
        for i in range(4, -1, -1):
            x = min(int(tmp/self.a[i]), self.banknotes[self.a[i]])
            # self.banknotes[self.a[i]] -= x
            # print(self.a[i], x, tmp)
            ans.append(x)
            tmp -= x * self.a[i]
        if tmp != 0:
            return [-1]
        ans = ans[::-1]
        for i in range(5):
            self.banknotes[self.a[i]] -= ans[i]
        return ans


# Your ATM object will be instantiated and called as such:
# obj = ATM()
# obj.deposit(banknotesCount)
# param_2 = obj.withdraw(amount)
```
