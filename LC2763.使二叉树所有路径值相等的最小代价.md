---
public: true
categories: problem
tags:
- LeetCode
- 树形 DP
date:
- 2024/02/28
permalink: problem/lc2763
title: LC2763.使二叉树所有路径值相等的最小代价
lastMod: 2024-10-05
toc: "true"
---

题目链接：
<!--more-->
## 题解
树形 DP，每一个节点最后对答案贡献是左右子树路径 sum 的差。
## 参考代码
```cpp
#
# @lc app=leetcode.cn id=2673 lang=python3
#
# [2673] 使二叉树所有路径值相等的最小代价
#

# @lc code=start
class Solution:
    def minIncrements(self, n: int, cost: List[int]) -> int:
        
        # ans = 0
        def dfs(rt, n):
            if rt > n:
                return 0, 0
            now_ans = 0
            left, ans = dfs(rt*2, n)
            now_ans += ans
            right, ans = dfs(rt*2+1, n)
            now_ans += ans
            now_ans += abs(left-right)
            
            return max(left, right) + cost[rt-1], now_ans
        
        sum1, ans = dfs(1, n)
        return ans
# @lc code=end


```
